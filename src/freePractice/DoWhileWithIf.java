package freePractice;

public class DoWhileWithIf {
    public static void main(String[] args) {
        String word = "HelloWorld";
        int j = word.length();
        int i = 0;
        if ( i <= word.length()) {
            System.out.print("" + word.charAt(i));
            i++;

        }
        if ( i <= word.length()) {
            System.out.print("" + word.charAt(i));
            i++;

        }
        if ( i <= word.length()) {
            System.out.print("" + word.charAt(i));
            i++;

        }
        if ( i <= word.length()) {
            System.out.print("" + word.charAt(i));
            i++;

        }
        if ( i <= word.length()) {
            System.out.print("" + word.charAt(i));
            i++;

        }
        if ( i <= word.length()) {
            System.out.print("" + word.charAt(i));
            i++;

        }
        if ( i <= word.length()) {
            System.out.print("" + word.charAt(i));
            i++;

        }
        if ( i <= j) {
            System.out.print("" + word.charAt(i));
            i++;

        }
        if (i <= j) {
            System.out.print("" + word.charAt(i));
            i++;

    }
        if (i <= j) {
            System.out.print("" + word.charAt(i));
            i++;

            }
                }
            }
